/*
import api from './api';
import type { Artist } from '../types';

export async function getArtists(): Promise<Artist[]> {
  try {
    const response = await api.get<Artist[]>('/rest/artists');
    return response.data;
  } catch (error) {
    throw new Error('Errore nel caricamento degli artisti');
  }
}*/