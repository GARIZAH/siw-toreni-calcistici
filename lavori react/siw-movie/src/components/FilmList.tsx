/*import { FilmCard } from './FilmCard'

export interface Film {
  id: number
  title: string
  year: number
  director: string
  rating?: number
}

interface FilmListProps {
  films: Film[]
}

export function FilmList({ films }: FilmListProps) {
  return (
    <div>
      
      <h2>Totale film: {films.length}</h2>

      {films.map((film) => (
        <FilmCard
          key={film.id} // key obbligatoria
          title={film.title}
          year={film.year}
          director={film.director}
          rating={film.rating}
        />
      ))}
    </div>
  )
}*/